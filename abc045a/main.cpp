#include <iostream>

using namespace std;

int main() {
    int a, b, h, output = 0;
    cin >> a;
    cin >> b;
    cin >> h;
    output = (a + b) * h / 2;
    cout << output << endl;
    return 0;
}